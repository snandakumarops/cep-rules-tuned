package com.myspace.ceptest;

import java.lang.Number;

rule "awda"
	dialect "mvel"
	when
		If there are > than 1 Transaction with "POS" and merchantType over 10m
	then
		insert fraud with reason "fraudReason"
end
