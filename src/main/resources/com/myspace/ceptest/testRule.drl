package com.myspace.ceptest;
import fraud.analysis.demo.transaction.Transaction;

rule "print test"
when
accumulate ($b:Transaction(transactionType == "POS", merchantType=="A433998") over window:time(1m) from entry-point Reference;$number: count($b))
then
System.out.println("hello here"+$number);
end
