package com.myspace.ceptest;

import java.lang.Number;

rule "SameTypeTxn"
	dialect "mvel"
	when
		If there are > than 1 Transaction with transaction type and merchantType over 10m
	then
		insert fraud with reason "Same type transaction in the last few minutes"
end
