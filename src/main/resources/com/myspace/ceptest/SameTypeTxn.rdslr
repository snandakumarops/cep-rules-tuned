package com.myspace.ceptest;

import java.lang.Number;

rule "SameTypeTxn"
	dialect "mvel"
	when
		If there are > than 2 Transaction with transaction type and merchantType over 10m
	then
		insert fraud with reason "More than 2 txn with same merchant in 10 minutes"
end
