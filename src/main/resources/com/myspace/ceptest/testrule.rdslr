package com.myspace.ceptest;

import java.lang.Number;

rule "testrule"
	dialect "mvel"
	when
		If there are > than 2 Transaction with "POS" and "A0900983" over 1m
	then
		insert fraud with reason "more than 2 txns in a min"
end
