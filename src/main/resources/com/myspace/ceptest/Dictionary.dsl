[condition]If there are > than {n} Transaction with "{transactionType}" and "{merchantType}" over {t}m=accumulate ($b:Transaction(transactionType == "{transactionType}", merchantType=="{merchantType}") over window:time({t}m) from entry-point Transactions;$number: count($b),$list: collectList($b);$number > {n})$c2: Transaction() from $list
[consequence]insert fraud with reason "{fraudReason}"=System.out.println("hello");CEPFraud cepFraud = new CEPFraud();cepFraud.setTransaction($c2);cepFraud.setFraudReason("{fraudReason}");insert(cepFraud);